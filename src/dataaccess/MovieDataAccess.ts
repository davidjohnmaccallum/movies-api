/**
 * In a real application the database access code would go here.
 * To save time and make the example easier to run I have left
 * this out.
 */

import { MovieDataAccessMock } from "./MovieDataAccessMock";

export class MovieDataAccess extends MovieDataAccessMock {}